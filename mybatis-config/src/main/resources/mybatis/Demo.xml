<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petter.mapper.DemoMapper">

    <!--  insert 语句. -->
    <insert id="saveDemo" parameterType="com.petter.entity.Demo" useGeneratedKeys="true" keyProperty="id">
        insert into demo (name) values (#{name})
    </insert>

</mapper>